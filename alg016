package main

import (
	"fmt"
	"strings"
)

func main() {
	var x, y string

	fmt.Print("digite duas strings: ")
	fmt.Scan(&x, &y)
	
	if strings.Contains(x,y) {
		fmt.Println("A segunda é uma substring da primeira")
	} else {
		fmt.Println("A segunda não é uma substring da primeira")
	}

	
}
